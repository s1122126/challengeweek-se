package enemy;

import enemy.Enemy;

public class Piraten extends Enemy {
    public Piraten(String health, String image, int damageAmount, int movementSpeed, int spawnX, int spawnY) {
        super(health, image, damageAmount, movementSpeed, spawnX, spawnY);
    }
}
