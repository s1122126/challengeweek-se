package enemy;

import enemy.Enemy;

public class Samurai extends Enemy {
    public Samurai(String health, String image, int damageAmount, int movementSpeed, int spawnX, int spawnY) {
        super(health, image, damageAmount, movementSpeed, spawnX, spawnY);
    }
}
