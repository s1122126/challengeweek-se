package enemy;

import enemy.Enemy;

public class Samurai extends Enemy {
    public Samurai(int health, int damageAmount, int movementSpeed, String image, int spawnX, int spawnY) {
        super(health, damageAmount, movementSpeed, image, spawnX, spawnY);
    }
}
